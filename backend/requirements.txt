fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.0
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt>=4.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
pydantic[email]>=2.5.0
httpx>=0.25.0
# ── ML: Semantic similarity search ──────────────────────────────────────────
# fastembed uses ONNX runtime (no PyTorch) — ~150MB RAM, fits Render free tier
# Same all-MiniLM-L6-v2 model, identical 384-dim embeddings to sentence-transformers
# Optimization: add faiss-cpu>=1.7.4 for O(log n) ANN at 10k+ tickets
fastembed>=0.2.0
numpy>=1.24.0
# ── Phase 2: Qdrant in-process vector DB ────────────────────────────────────
qdrant-client[local]>=1.9.0
# ── Phase 3: LangChain LLM chain abstraction (Groq — free, no credit card) ──
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-groq>=0.2.0
# ── Phase 4: Embedding fine-tuning (scripts/finetune_embeddings.py) ─────────
sentence-transformers>=2.2.0
datasets>=2.0.0
accelerate>=1.1.0
# ── Phase 5: DistilBERT priority classifier (scripts/train_priority_classifier.py)
transformers>=4.35.0
torch>=2.0.0
